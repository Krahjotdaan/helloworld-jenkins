pipeline {
    agent {
        docker {image 'python:3.10'}
    }
    stages {
        stage('Task2') {
            steps {
                bash '''
                    python3 -m venv venv 
                    source venv/bin/activate
                    python -m unittest test.py
                '''
            }
        }
    }
}